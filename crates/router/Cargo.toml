[package]
name = "spec-db-router"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Query intent classification and routing for spec-db"

[dependencies]
spec-db-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
spec-db-causal = { workspace = true }
spec-db-search = { workspace = true }

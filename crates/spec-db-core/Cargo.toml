[package]
name = "spec-db-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Core domain types, traits, and error hierarchy for spec-db"

[dependencies]
serde = { workspace = true }
serde_yml = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3"
